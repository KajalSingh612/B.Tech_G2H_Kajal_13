#include<stdio.h>
#include<math.h>
int main ()
{
    float a,b;
    printf("enter two numbers \n");
    scanf("%f %f" ,&a,&b);
    if (a==b)
    { 
    printf("two numbers are equal");
    }
    else
    {
    printf("numbers are not equal");
    }
return 0;
}